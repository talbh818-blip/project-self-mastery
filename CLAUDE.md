# פרויקט מחויבות לעצמי — מסמך אפיון (CLAUDE.md)

> קובץ זה הוא ה"זיכרון" הקבוע של הפרויקט. קלוד קוד קורא אותו אוטומטית בכל סשן.
> הוא מגדיר את החזון, הארכיטקטורה, וההחלטות שכבר התקבלו. אין לסטות ממנו בלי אישור.

---

## 1. סקירה כללית

**שם האפליקציה:** פרויקט מחויבות לעצמי
**לוגו:** מצפן
**מטרה:** לעזור לאנשים לבנות ולשמר הרגלים חיוביים — ולהשמיד התמכרויות שליליות — במהלך השבוע.
**שפה:** עברית בלבד, **RTL מלא** (כל הממשק מימין לשמאל).
**פלטפורמה:** Web App רספונסיבי, **מותאם בעיקר ל‑Mobile** אבל עובד גם ב‑Desktop. לבנות כ‑PWA (ניתן "להתקין" על מסך הבית).

### חזון רב‑שלבי (חשוב להבנת ההחלטות)
- **שלב א' (נוכחי):** Web App. אישי לגמרי — לכל משתמש הנתונים שלו.
- **שלב ב' (עתיד):** הוספת שכבה חברתית מלאה (מסך "משתתפים" — ראה נספח "מסכים מושהים").
- **שלב ג' (עתיד):** מעבר/הרחבה לאפליקציית מובייל native — שבה ה"חוסם" **באמת** יחסום אפליקציות (ראה נספח "מסכים מושהים").

> **עיקרון מנחה:** לבנות את הכל בצורה מודולרית כך שכל שלב מתווסף בלי לבנות מחדש. שכבת ה‑data access צריכה להיות מסודרת כדי שיהיה קל להוסיף את המסכים המושהים בעתיד בלי שכתוב של מה שקיים.

---

## 2. מחסנית טכנולוגית (Tech Stack)

- **Frontend:** מסגרת web מודרנית (React מומלץ). רספונסיבי, mobile-first.
- **עיצוב:** ראה סעיף 4. להשתמש ב‑**Lucide** לאייקונים (פלאט, קווי, מינימליסטי) בשלב הנוכחי — בהמשך יוחלפו במאגר אייקונים מותאם.
- **אימות (Auth):** התחברות בעזרת **חשבון Google** (OAuth). המשתמש לעולם לא מקליד סיסמה באפליקציה.
- **בסיס נתונים + Backend:** **Supabase**. כל הנתונים של כל משתמש נשמרים ב‑Supabase תחת ה‑user id שמגיע מ‑Google OAuth.
- **PWA:** להגדיר manifest + service worker כדי שניתן יהיה להתקין על מסך הבית.

### הערות ארכיטקטורה
- Supabase Auth מטפל ב‑Google OAuth. אחרי התחברות, נוצרת/נטענת רשומת משתמש.
- **Row Level Security (RLS):** חובה. כל משתמש ניגש אך ורק לנתונים שלו. במיוחד החזון (journaling) — פרטי לחלוטין, לעולם לא נגיש למשתמש אחר (ראה סעיף עקרונות פרטיות).
- לבנות שכבת data access מסודרת כדי שיהיה קל להוסיף את התכונות החברתיות בשלב ב'.

---

## 3. עקרונות פרטיות (קריטי)

1. **החזון (כתיבה חופשית) הוא פרטי כברירת מחדל — לעד.** כל רשומת חזון נוצרת במצב `private` ונשארת כך אלא אם המשתמש **בוחר אקטיבית** לשתף אותה. שיתוף הוא **opt-in פר-רשומה** בלבד — אין מצב שבו כתיבה הופכת לחשופה לאחרים ללא פעולה מודעת של המשתמש. ברירת המחדל של המערכת חייבת תמיד להישאר פרטית; אם חוסר ודאות — פרטי.
2. ה-schema של `vision_entries` כולל עמודת `visibility` עם הערכים `private` / `public` / `specific` כתשתית עתידית, אבל **ב-V1 ה-UI לא חושף בקרות שיתוף בכלל**, וה-RLS מאפשר גישה רק לבעלים (ולאדמין למודרציה, כמו בטבלאות אחרות). כשמוסיפים שיתוף — מוסיפים policies חדשות שקוראות את העמודה הזו.
3. במסך "משתתפים" (אם יחזור) נחשפים **אך ורק נתונים** — ניקוד, מספר הרגלים שנבנו, הצלחות. **לעולם לא** תוכן כתיבה, גם לא כשהמשתמש בחר "פומבי".
4. בשלב ב' יתווסף לכל משתמש שליטה גרעינית על מי רואה כל רשומה (פרטי / פומבי באפליקציה / משתמשים ספציפיים). בשלב הנוכחי (התשתית) — המבנה כבר פתוח לכך.

---

## 4. שפה עיצובית

- **תחושה כללית:** רגועה, מינימליסטית, נקייה. הרבה מרחב נשימה.
- **פלטה:**
  - **ירוק יער עמוק** — צבע ראשי (כותרות, כפתורים ראשיים, הדגשות).
  - **לבן שמנת (cream/off-white)** — רקעים.
  - צבעים נוספים מינימליים ושקטים בלבד.
- **סגנון:** Flat. אייקונים קוויים/מלאים מינימליסטיים, צבע אחד.
- **טיפוגרפיה:** נקייה, נוחה לקריאה. במסך החזון — נוחה גם לקריאה ארוכה (תחושת "מחברת").
- **השראה:** סגנון בסטייל של אפליקציות הרגלים מינימליסטיות מודרניות (ירוק/קרם, פלאט, נקי).
- חשוב: עיצוב מותאם RTL מההתחלה — אייקונים, חיצים, יישור טקסט.

---

## 5. ניווט

תפריט תחתון (bottom navigation) עם **4 פריטים** למשתמש רגיל:

1. הרגלים (Home)
2. חזון
3. קורס
4. משתמש

> למשתמש אדמין מתווסף **5. ניהול** משמאל ל"משתמש".

> מסך הבית בעת הכניסה הוא **"הרגלים"**.

> הערות:
> - מסך "נתונים" הוסר מהניווט — תצוגת הנתונים/דשבורד משולבת בתוך מסך "הרגלים".
> - המסכים "חוסם" ו"משתתפים" **הוסרו מהניווט** בשלב הנוכחי כיוון שלא בשימוש. ייתכן ויחזרו בעתיד — הסעיפים המפורטים שלהם נשמרים בנספח "מסכים מושהים" בסוף המסמך.

---

## 6. סדר בנייה

לבנות **מסך‑מסך**, לא הכל בבת אחת. מתחילים מ**"הרגלים"** עד שהוא עובד מצוין, ורק אז ממשיכים. סדר מומלץ:
1. תשתית: Auth (Google) + Supabase + ניווט תחתון + עיצוב בסיסי. ✅ קיים.
2. **הרגלים** (הלב של האפליקציה, כולל דשבורד הנתונים המשולב בתוכו). 🚧 בבנייה.
3. משתמש (מרכז שליטה אישי — פרופיל, ערכת נושא, פידבק/תמיכה, יציאה). ✅ קיים.
4. ניהול (אדמין בלבד — ניהול משתמשים ופעילות). ✅ קיים.
5. חזון.
6. קורס.

> "חוסם" ו"משתתפים" מושהים — ראה נספח בסוף.

---

# פירוט המסכים

## מסך 1: הרגלים (Home) — הלב של האפליקציה

### מבנה הטבלה
- **טבלת הרגלים שבועית** עם **5 סלוטים** ל‑5 הרגלים.
- כל סלוט = הרגל אחד, עם **אייקון** שמסמן אותו בראש העמודה.
- שורות = ימי השבוע. עמודות = ההרגלים (או להפך, מה שנוח ל‑RTL ולמובייל — לתכנן לקריאות).
- ניתן **לדפדף אחורה בין שבועות**. גם קדימה — **יום אחד בלבד** קדימה (אפשר לסמן את "מחר", אבל הוא לא נספר בנתונים עד שהוא מגיע בפועל).

### מעל הטבלה
- **שם השבוע + טווח התאריכים** שלו (בקטן).
- **TOTAL SCORE** — הניקוד הכולל של המשתמש (ראה שיטת ניקוד).

### סימון הימים — שלושה מצבים
- **V** — הצלחה: בנית הרגל חיובי, או השמדת התמכרות שלילית. **V הוא תמיד חיובי** (גם בהרגל "לבנות" וגם בהתמכרות "להשמיד").
- **X** — כישלון.
- **מקף ( – )** — ריק / עדיין לא סומן.
- ניתן לסמן את **היום הנוכחי** וגם **ימים אחורה** (אם שכחת).

### סוגי הרגלים
- **חיובי ("לבנות")** — למשל לקום מוקדם, לקרוא.
- **שלילי ("התמכרות להשמיד")** — למשל להפסיק לעשן.
- בשני הסוגים, **V = יום מוצלח**. הסוג משמש להבנה/הצגה, אבל הניקוד זהה.

### מאגר ההרגלים
- ההרגלים מגיעים מ**מאגר מנוהל** (על ידי בעל האפליקציה). כל פריט במאגר: **שם + סוג (חיובי/שלילי) + אייקון**.
- המשתמש **בוחר** הרגל מתוך המאגר לכל סלוט.
- בנוסף — אפשרות **"אחר"**: המשתמש מזין שם חופשי ובוחר אייקון גנרי (הרגל מותאם אישית).
- בשלב הנוכחי המאגר קטן/זמני — בעל האפליקציה יוסיף הרגלים לפי בקשות משתמשים. **לבנות את המאגר כטבלה ב‑Supabase** שקל להוסיף לה פריטים.

### החלפת הרגל והיסטוריה (חשוב!)
- המשתמש בוחר 5 הרגלים, אבל **תמיד יכול להחליף/לשנות** הרגל בסלוט.
- **כל הרגל הוא ישות עצמאית עם היסטוריה משלו.** כשמחליפים הרגל בסלוט, ההיסטוריה של ההרגל הישן **נשמרת** (לא נמחקת).
- **הטבלה ההיסטורית מציגה מה שהיה באמת באותו שבוע.** דוגמה: אם בשבוע שעבר עקבת אחרי "ריצה" בסלוט 3, חזרה אחורה לאותו שבוע תציג "ריצה" — גם אם היום בסלוט 3 יש "קריאה". הטבלה היא תמונת מצב היסטורית אמיתית, לא המצב הנוכחי שמוחל על העבר.

### ניקוד פר‑הרגל
- מתחת לכל הרגל בטבלה מוצג **כמה נקודות הוא צבר**.

---

## שיטת הניקוד (מדויק — לא להמציא מספרים!)

### בסיס
- **V = +5 נקודות.**
- **X = −3 נקודות** (כולל X אוטומטי — ראה למטה).
- **מקף (ריק) = 0 נקודות.**

### X אוטומטי
- יום שנשאר **ריק (מקף) במשך יומיים** → הופך **אוטומטית ל‑X**.
- ה‑X האוטומטי מתנהג בדיוק כמו X רגיל: **−3 נקודות** ו**שובר רצף**.

### בונוסי רצף (streak) — חד‑פעמיים, פר‑הרגל
- הבונוס ניתן **פעם אחת** כשמגיעים לאבן דרך (לא מתמשך).
- הרצף נספר **פר‑הרגל**.
- אבני דרך (ערכי ברירת מחדל — לכוונון בהמשך):
  - **7 ימים רצוף = +20**
  - **14 ימים רצוף = +40**
  - **30 ימים רצוף = +100**
- **רצף נשבר על ידי X** (כולל X אוטומטי).

> הערך המספרי המדויק של הבונוסים נתון לכוונון עתידי — לבנות אותם כקבועים שקל לשנות במקום אחד בקוד.

### TOTAL SCORE
- **מצטבר לנצח. אף פעם לא מתאפס.** מספר אחד גדול שרק גדל (או יורד עם X).
- שים לב: X (−3) קטן מ‑V (+5), כך שיום מוצלח שווה יותר מיום כושל. זו החלטה מודעת.

---

## דשבורד הנתונים (משולב בתוך מסך "הרגלים")

> **מסך נתונים נפרד הוסר מהניווט.** דשבורד הנתונים משולב בתוך מסך "הרגלים". נבנה **מודולרי** כך שקל להוסיף "כרטיס נתונים" חדש בלי לשבור קיים. הפרטים יתגבשו מול נתונים אמיתיים.

### מה שכבר ברור
- **לב הדשבורד:** **גרף קו שעולה/יורד לאורך זמן** בהתאם לביצועי ההרגלים (כנראה TOTAL SCORE או אחוז הצלחה — להחליט תוך כדי).
- **בורר תצוגה** למעלה: **השבוע / החודש / 3 חודשים אחרונים / כל הזמנים**.

### מועמדים לכרטיסים עתידיים (לא לבנות עדיין אלא אם קל)
- אחוז הצלחה כללי לתקופה.
- פירוט אחוז הצלחה פר‑הרגל ("איפה נפלת").
- הרצף הכי ארוך לכל הרגל (שיא אישי).
- היום החזק/החלש בשבוע.
- מפת חום (heatmap) של לוח שנה.

---

## מסך 2: חזון

> מסך journaling. מרחב **נפרד לחלוטין** מההרגלים. התחושה: מחברת אישית רגועה ושקטה, לא טופס. **פרטי כברירת מחדל** (ראה עקרונות פרטיות).

### V1 — מה נבנה
- **3 טאבים בראש המסך:** שנתי / חודשי / שבועי. מעבר ביניהם מיידי. כל טאב זוכר את התקופה שבה היה המשתמש לאחרונה.
- **ניווט תקופות:** מתחת לטאבים — חצים `<` / `>` למעבר תקופה אחורה/קדימה + הצגת שם התקופה הנוכחית (`שנת 2026`, `מאי 2026`, `25 מאי – 31 מאי 2026`). לחיצה על שם התקופה כשהיא לא הנוכחית מחזירה ל"עכשיו".
- **חסימת עתיד:** אי-אפשר לכתוב חזון לתקופה שעוד לא התחילה. שבוע הבא / חודש הבא / שנה הבאה מוצגים כ-`Locked`.
- **עורך טקסט עשיר (Tiptap):**
  - bold, רשימה עם נקודות, רשימה ממוספרת.
  - **Highlighter** עם 4 צבעים: צהוב / כתום / כחול / אדום.
  - RTL מלא (caret, יישור, placeholder).
  - תחושת מחברת: line-height גדול, הרבה padding, ללא frames מיותרים.
- **שמירה אוטומטית** דרך debounce של 1 שנייה. אינדיקטור בפינה: `שומר…` / `נשמר` / `שגיאה בשמירה`. בלי כפתור "שמור".
- **אייקון פר-חזון:** כפתור ב-`DateBar` (משמאל ל"כתיבה מודרכת") פותח `VisionIconPicker` — גיליון בחירת אייקון/אימוג'י (מאגר ההרגלים: `HABIT_ICONS` + `HABIT_EMOJIS`, מוצג דרך `HabitIcon`). הבחירה נשמרת לעמודה `icon` של ה-`vision_entry` ומוצגת **ליד כותרת כל שלוש השורות** ב-`VisionLayers` (כל רמה — שנתי/חודשי/שבועי — עם האייקון שלה, באופן קבוע). הטעינה: `fetchVisionRowMeta` מביא את 3 השורות (icon + content) של ה-anchor הנוכחי בקריאה אחת; הבחירה אופטימית + `updateVisionEntryIcon`. מיגרציה: `0021_vision_entry_icon.sql`.
- **כתיבה מודרכת (Assist):** טוגל ב-`DateBar` חושף כפתור "+ כתיבה מודרכת" שמכניס שאלה מנחה אקראית (בלי חזרות בתוך אותה רשומה). **מאגר השאלות מנוהל בטבלת `vision_questions`** (מיגרציה `0035`) — פר-scope (שנתי/חודשי/שבועי), עם fallback לקטלוג סטטי ב-`questions.ts` עד שה-fetch נטען (או אם נכשל/ריק). האדמין מנהל את השאלות בטאב **"שאלות"** בפאנל הניהול (`VisionQuestionsAdminPanel` — הוספה/עריכה/מחיקה). טקסט השאלה נשמר כ-snapshot על ה-node ברשומה — עריכת/מחיקת שאלה במאגר לא משנה רשומות קיימות. עיצוב הבלוק: פס מבטא ירוק דק בצד ימין + טקסט בגוון ירוק (`forest-600`), בלי קופסה.
- **סימן "נכתב" (check-mark):** חזון שיש בו תוכן טקסט אמיתי מציג עיגול ירוק עם וי לבן בתחילת שורת הכותרת (מימין לאייקון ולכותרת). מתעדכן **חי** תוך כדי כתיבה/מחיקה (`isVisionContentEmpty` ב-`content.ts`, דרך `onChange` של העורך) — נמחק כל התוכן → הוי נעלם.
- **שורת השבוע ב-`VisionLayers`:** "שבוע N" מוצג ב-pill עגול (`bg-forest-700/30`), ולצידו טווח התאריכים של השבוע (`1.4 – 7.4`) ב-`text-ink-300/80` מעומעם ו-`dir="ltr"`. טווח התאריכים מחושב מ-`parsePeriodStart('weekly', ...)` + 6 ימים כדי להתאים לגבולות השבוע האמיתיים.

### מודל נתונים (`vision_entries`)
- מפתח: `(user_id, scope, period_key)` ייחודי.
- `scope`: `'yearly' | 'monthly' | 'weekly'`.
- `period_key`:
  - שנתי: `YYYY` (למשל `2026`).
  - חודשי: `YYYY-MM` (למשל `2026-05`).
  - שבועי: `YYYY-MM-DD` — התאריך של **יום ראשון** שפותח את השבוע (למשל `2026-05-31`). השבוע רץ ראשון→שבת (השבוע הישראלי). שבוע **שייך לכל חודש שטווח 7 הימים שלו חופף אליו** — כך שבוע-גבול (למשל 31.5–6.6) מופיע גם בסוף מאי וגם בתחילת יוני ומשותף ביניהם. המשמעות: השבוע הנוכחי תמיד "פתוח" תחת החודש הנוכחי גם לפני שיום ראשון שלו נכנס לאותו חודש. (חודש מחזיק 4–6 שבועות; שבועות גבול משותפים.)
- `content`: jsonb — Tiptap document JSON.
- `visibility`: `'private'` כברירת מחדל (ראה סעיף פרטיות). ב-V1 לא נחשף ב-UI.
- מודול אחד ויחיד למפתחות התקופות: `src/features/vision/period.ts`.

### עתידיות (לא ב-V1) — כן לתכנן, לא לבנות עדיין
- **העלאת תמונות** לתוך הכתיבה (דורש Supabase Storage bucket `vision-images` + RLS).
- **כפתור Assist** — toggle לכתיבה מודרכת עם שאלות מנחות ב-bold ש-(א) ניתן לסמן עליהן X או (ב) לבקש שאלה אחרת. לתכנן כך שהשאלות הן block נפרד שאפשר להזריק לעורך.
- **הגדרות פרטיות פר-רשומה** (`private` / `public` / `specific`) — מצריך RLS policies חדשות + UI sheet + טבלת `vision_shares` למצב `specific`.
- **תיוג** + **journaling יומי** — שכבת רשומות יומיות נפרדות מ-3 הרמות, עם תגיות חופשיות.
- **מבנה מקופל היררכי** (שנים → חודשים → שבועות בתצוגת עץ) — כרגע ניווט שטוח דרך טאבים + חצים.

### עיצוב
- הרבה מרחב לבן, טיפוגרפיה נוחה לקריאה ארוכה, תחושת מחברת. צבעים מהפלטה הקיימת בלבד (`forest-700` לפעיל, `surface-card` לרקעים, `ink-100/300/500` לטקסט).

---

## מסך 3: קורס

> בשלב הנוכחי: רשימת סרטונים שבעל האפליקציה מעלה.

- תצוגה: רשימת סרטונים (תמונה ממוזערת + כותרת). לחיצה → נגן.
- **אירוח הווידאו:** **YouTube כ‑Unlisted** (לא רשום), מוטמע (embed) באפליקציה. חינמי ומהיר; בלי עלויות אחסון וידאו בשלב זה.
- לבנות כך שקל להוסיף סרטונים (טבלה ב‑Supabase עם כותרת + מזהה/קישור יוטיוב + תמונה ממוזערת).

---

## מסך 4: משתמש

> מרכז שליטה אישי של המשתמש. מסך שכבר נבנה ופעיל. ✅

### מה יש בו
- **תמונת פרופיל (avatar):** ניתנת להעלאה ע"י המשתמש (Storage ב‑Supabase). אם אין — מוצג אייקון ברירת מחדל.
- **שם תצוגה:** מ‑Google OAuth (או מהשדות המוזנים).
- **פרטים אישיים (עריכה ושמירה):** שם פרטי, שם משפחה, טלפון. אימייל **קריאה בלבד** (מגיע מ‑Google, לא ניתן לשינוי).
- **תצוגה (ערכת נושא):** בורר **מצב כהה / מצב בהיר**. עדכון אופטימי (מיידי) + נשמר לפרופיל ול‑localStorage.
- **שלח פידבק / בקש תמיכה:** פותח גיליון (sheet) שיוצר "טיקט" שנשמר ל‑Supabase. בעל האפליקציה רואה ועונה דרך הניהול/אימייל.
- **קישורים משפטיים:** מדיניות פרטיות (`/privacy`) ותנאי שימוש (`/terms`) — דפים נפרדים תחת ה‑Layout.
- **התנתקות (sign out).**

### עיצוב
- כל בלוק בכרטיס נפרד (`bg-surface-card rounded-2xl`), הפרדה ברורה.
- משתמש בטוקני העיצוב של ערכת הנושא (תומך אוטומטית בכהה/בהיר).

---

## מסך 5: ניהול (אדמין בלבד) 🔒

> מסך לבעל האפליקציה בלבד. מוגן ב‑`AdminRoute` שבודק את שדה ה‑role/admin בפרופיל. ✅

### מה יש בו
- **רשימת כל המשתמשים** עם נתוני פעילות (rollup) — ניקוד, פעילות אחרונה, מספר עצים שתולים וכו'.
- פעולות אדמין על משתמש: **חסימה/הסרת חסימה** (`blocked`), **עריכת מספר עצים שתולים**, **מחיקת פעילות**, ועוד.
- **BlockedGate:** משתמש שסומן כ‑`blocked` רואה מסך חסימה במקום האפליקציה.
- **טאב "שאלות":** ניהול מאגר השאלות המנחות של החזון (`vision_questions`) — הוספה/עריכה/מחיקה פר-scope (שנתי/חודשי/שבועי). ראה סעיף "כתיבה מודרכת" במסך החזון.

### תוספות עתידיות (לא לבנות עדיין)
- ניהול מאגר ההרגלים (CRUD) דרך הממשק.

---

## תכונת רוחב: עץ צמיחה ("העץ שלי" / "החלקה שלי") 🌳

> תכונת ויזואליזציה שמלווה את המשתמש (קומפוננטה `TreeCard`). מוצגת בראש מסך "הרגלים" כשורה דקה; לחיצה עליה פותחת את פופאפ "החלקה שלי" המלא. ✅

### החזון
המשתמש "מגדל" עץ דיגיטלי לאורך זמן באמצעות הניקוד שהוא צובר. כשהעץ מבשיל — המשתמש משתיל אותו בחלקה האישית שלו ומתחיל לגדל את הבא. **על כל עץ דיגיטלי שהמשתמש משתיל בחלקה, בעל האפליקציה תורם באופן פרטי לעמותה לשתילת עץ אמיתי באפריקה.** התרומה נעשית מאחורי הקלעים — אין קישור חיצוני באפליקציה ואין חיוב למשתמש. הסכומים יחושבו על בסיס סך השתילות הדיגיטליות בסוף כל חודש.

### שלבי גדילה (5 שלבים)
| שלב | תווית | סף (יחס ל-cycle target) |
|---|---|---|
| 0 | זרע (Sprout) | 0% |
| 1 | שתיל (Seedling) | 15.4% (100/650) |
| 2 | עץ צעיר (Sapling) | 38.5% (250/650) |
| 3 | עץ גדל (YoungTree) | 69.2% (450/650) |
| 4 | עץ בשל (MatureTree) | 100% — מוכן לשתילה |

> ספי השלבים מבוטאים כיחס ל-650 ומקבלים קנה-מידה דינמי לפי ה-`cycleTarget` של העץ הנוכחי. הקבוע: `STAGE_RATIOS` ב-`TreeCard.tsx`.

### Cycle Targets — כמות ניקוד פר עץ
- **עץ ראשון (index 0):** 200 נקודות (הנחת ברוכים-הבאים)
- **עץ שני (index 1):** 400 נקודות (הנחה רכה)
- **עץ שלישי ואילך:** 650 נקודות (סטנדרט)

> קבועים: `CYCLE_TARGETS = [200, 400]` עם `DEFAULT_CYCLE_TARGET = 650`.

### Floor — חישוב התקדמות שמנותק ממונה העצים
- **בעיה היסטורית:** קודם, התקדמות העץ הנוכחי חושבה כ-`totalScore − sum(cycleTargetFor(i) for i in 0..trees_planted-1)`. זה גרם לבאג קריטי: עדכון `trees_planted` ע"י אדמין דרך פאנל הניהול היה דורס למשתמש את המד (אם אדמין הוסיף 48 עצים, המערכת "הניחה" 31,200 נק' שנצרכו → המד קפץ ל-0%).
- **פתרון:** עמודה חדשה `cycle_score_floor` שמעודכנת **רק** כשהמשתמש שותל בעצמו. נוסחת המד: `cycleScore = max(0, totalScore − cycle_score_floor)`.
- **כל שתילה מאפסת את המד ל-0%:** ברגע שתילה, `cycle_score_floor` מתעדכן ל-`totalScore` הנוכחי (לא קופץ ב-cycleTarget בלבד). **עודף ניקוד מעל הסף הולך לאיבוד** — החלטה מודעת כדי שהמד תמיד יחזור ל-0% אחרי שתילה ולא יאפשר "rapid-fire planting" כשיש ניקוד בנק.

### הפופאפ "החלקה שלי"
חלקה איזומטרית מצוירת ב-SVG עם דשא וקירות צד. מתפתחת לאורך זמן:

| רכיב | תיאור |
|---|---|
| **כותרת** | "על כל עץ דיגיטלי שתשתול כאן — ישתל עץ אמיתי באפריקה 🌱" (16px, שתי שורות מפורשות) |
| **תא מרכזי** | העץ הנוכחי הגדל. גודל משתנה לפי stage: 26→36→46→56→64 פיקסלים (יחידות viewBox). |
| **תאים פריפריאליים** | עצים בוגרים ששתל המשתמש (42px קבוע) + zerים-placeholders בתאים ריקים (18px). |
| **מד התקדמות** | בר ירוק עם תווית "X / Y" + אחוז, ותווית שלב ("זרע", "שתיל" וכו'). |
| **ניקוד כולל** | מופיע למטה. |
| **שורת פעולה** | כשהעץ בשל: שני כפתורים — "🌍 שתול את העץ שלך" (עם shimmer) + "המשך". אחרת: רק "המשך" ברוחב מלא. |

### הרחבת גריד (Zoom-out מתקדם)
ככל שהמשתמש שותל יותר עצים, הגריד מתרחב והעצים נראים קטנים יותר (אפקט zoom-out):

| גריד | מקסימום עצים |
|---|---|
| **5×5** | 24 |
| **7×7** | 48 |
| **9×9** | מעבר ל-48 |

מימוש: הקבוע `GRID_TIERS` ב-`TreeCard.tsx`. גודל התאים נשאר קבוע ביחידות viewBox; כשהגריד גדל, ה-SVG viewBox מתרחב, ולכן כל תא תופס אחוז קטן יותר מרוחב הקונטיינר → הצמצום ויזואלי באטו-מטיק. מעבר tier מקבל transition של 800ms על aspect-ratio.

### Drag and Drop של עצים בחלקה
המשתמש יכול לסדר מחדש את עצי החלקה:

- **לחיצה ארוכה (500ms)** על עץ בוגר → נכנס למצב גרירה (scale 1.1 + drop shadow + opacity 0.92).
- **תזוזה לפני 500ms** מבטלת את הלחיצה הארוכה (מאפשר scroll רגיל של הדף).
- **טבעת snap צהובה** מסביב לתא היעד בזמן גרירה. **אדומה** מעל המרכז (סימן דחייה).
- **שחרור:**
  - **על תא ריק** → העץ נשתל שם.
  - **על תא תפוס** → **swap** (החלפת מקומות בין שני העצים).
  - **על המרכז** → דחייה (המרכז שמור לעץ הגדל).
  - **על אותו תא** → no-op (בלי כתיבה ל-DB).
- **persistence:** ה-pointer listeners ברמת `document` (לא על כל עץ) — כך שהשחרור עובד גם בתאים ריקים שהם sprout placeholders עם `pointer-events: none`. המיקומים נשמרים ל-`tree_placements`.

### אנימציות
- **Shimmer על כפתורי השתילה:** פס לבן דק ובהיר עם קצוות חדים (`linear-gradient` עם 0% transition stops) עובר משמאל לימין, lop של 4.2s עם `cubic-bezier(0.72, 0, 0.28, 1)`. אפקט "מבריק" קרטוני כמו מדבקה. CSS class: `.btn-shimmer`.
- **קונפטי שתילה:** כשהמשתמש לוחץ "שתול" — פיצוץ קונפטי **מקרקעית המסך כלפי מעלה** (סגנון Duolingo), 60 חלקיקים, 7 צבעים, 1.8s. fullscreen `fixed z-[60]`. הקאמפוננטה: `BottomConfetti`.
- **הגדלה הדרגתית:** העץ המרכזי מתחיל קטן (26px ב-Sprout) ומתפתח עד 64px (Mature) — כך שלא נראה גדול יותר מהבוגרים השתולים סביב (42px). הסדר: `CENTER_SIZE_BY_STAGE` ב-`TreeCard.tsx`.

### Emoji preload
ה-🌍 (כדור הארץ) שעל הכפתור משך זמן רב לטעון בעבר בגלל שגיאת case ב-fluent emoji map (`Globe showing Europe-Africa` במקום `Globe showing europe-africa` — תיקייה אמיתית עם e קטנה). הגיב 403 ונפילה ל-CDN שני. תוקן + הוסף `<link rel="preload">` ל-3 האימוג'ים הקריטיים (🌍 🌱 🎉) ב-`index.html`.

### מודל נתונים (Supabase)
טבלת `profiles` — ראה מיגרציות 0011, 0013, 0014:

| עמודה | סוג | תיאור |
|---|---|---|
| `trees_planted` | `int` | מונה כללי של עצים שהמשתמש שתל. אדמין יכול לעדכן ידנית. |
| `tree_placements` | `jsonb` | מערך מיקומים יחסיים למרכז: `[{di, dj}, ...]`. אורך עשוי להיות קטן מ-`trees_planted` (משתמשים ישנים) — אז fallback לסדר default. |
| `cycle_score_floor` | `int` | ניקוד הצבר שכבר "נצרך" לשתילה. נוסחת המד: `max(0, totalScore − cycle_score_floor)`. |

**מיגרציות רלוונטיות:**
- `0013_tree_placements.sql` — הוספת `tree_placements jsonb` עם CHECK של array.
- `0014_cycle_score_floor.sql` — הוספת `cycle_score_floor int` עם CHECK של non-negative.

### זרימת שתילה (atomic UPDATE)
לחיצה על "שתול את העץ שלך" → UPDATE יחיד ל-Supabase שמעדכן בו-זמנית:
1. `trees_planted += 1`
2. `tree_placements` ← מערך מורחב עם המיקום החדש (מחושב לפי הגריד **הבא** אחרי הרחבה, אם רלוונטי — תהליך atomic שלא יוצר אי-תאמה).
3. `cycle_score_floor` ← `totalScore` הנוכחי.

ה-update חוזר עם `.select().maybeSingle()` כדי לוודא ש-RLS לא חסם בשקט (`data: null` + `error: null` הוא תוצאה תקפה של דחיית RLS).

### אינטראקציה עם פאנל ניהול
- אדמין יכול לעדכן את `trees_planted` של משתמש דרך פאנל "ניהול".
- **חשוב:** עדכון `trees_planted` ע"י אדמין **לא נוגע** ב-`cycle_score_floor` ולא ב-`tree_placements`. המד של המשתמש לא נשבר.
- אם אדמין מגדיל מ-2 ל-50, המשתמש פשוט רואה 48 עצים נוספים בחלקה (במיקומי default מסדר ה-`cellsForGrid`). המד שלו ממשיך כרגיל.

### קבצים ראשיים
- `src/features/tree/TreeCard.tsx` — כל הלוגיקה (TreeCard, TreeFieldModal, IsometricField, BottomConfetti, geometry helpers, drag state machine).
- `src/index.css` — keyframes לאנימציות (`btn-shimmer-sweep`, `confetti-rise`).
- `supabase/migrations/0013_tree_placements.sql` + `0014_cycle_score_floor.sql`.

---

# נספח א': מסכים מושהים (אולי יבנו בעתיד)

> שני המסכים הבאים **הוסרו מהניווט ומהקוד** בשלב הנוכחי כיוון שלא היו בשימוש. הספצים נשמרים כאן ככלי תכנון אם נחליט להחזיר אותם בעתיד. **לא לבנות מחדש בלי אישור מפורש.**

## מסך מושהה 1: חוסם

> **מצב מתוכנן בעבר (Web):** ממשק מלא ופעיל שבו המשתמש מגדיר הכל, וההגדרות **נשמרות ב‑Supabase**. בפועל **לא חוסם** (דפדפן לא יכול לחסום אפליקציות אחרות במכשיר — זו מגבלה מהותית של web). כל ה"כוונות" שמורות ומוכנות.
> **מצב עתידי (native):** אותו ממשק בדיוק יבקש הרשאות מערכת ויחסום באמת. **לא לבנות פעמיים** — לתכנן כך שהמעבר חלק.

### זרימה
1. בכניסה הראשונה — האפליקציה **מבקשת את כל ההרשאות הנדרשות** (ב‑native פעיל אמיתית; ב‑Web נשמר כהגדרה/placeholder).
2. לאחר מכן כל משתמש **מגדיר את החוסם של עצמו**: אילו אפליקציות, ולכמה זמן.

### רשימת האפליקציות לחסימה
- **רשימה מנוהלת** (כמו מאגר ההרגלים): טיקטוק, אינסטגרם, יוטיוב, YouTube Shorts, Instagram Reels, פייסבוק וכו'. כל פריט עם אייקון.
- המשתמש בוחר מהרשימה אילו אפליקציות להגדיר. גם כאן אפשרות **"אחר"**.

### מגבלות (לכל אפליקציה)
- **מגבלה יומית:** סך הכל דקות מותרות ביום (למשל "טיקטוק מקס' 30 דק' ביום").
- **מגבלה שעתית:** **מקסימום 5 דקות בכל שעה** (מגביל את הקצב לאורך היום).

### טיימר שחרור (release timer)
- מנגנון **חיכוך מכוון** שנותן זמן להתחרט.
- זרימה: לחיצה על **"שחרר"** → **ספירה לאחור** שהוגדרה מראש → האפליקציה **נפתחת** למשך זמן שהמשתמש בחר מראש, **בטווח 1 דקה עד שעה** → ואז **ננעלת מחדש**.

---

## מסך מושהה 2: משתתפים

> השכבה החברתית.

- רואים **משתתפים אחרים** ונתונים עליהם: **הניקוד שלהם, כמה הרגלים בנו, הצלחות**.
- **בעבר תוכנן:** כולם רואים את כולם (לוח תוצאות פתוח).
- **בשלב ב' (אם יחזור):** כל משתמש יוכל לבחור מי רואה את הנתונים שלו. לתכנן את המבנה מראש לכך.
- **קריטי:** נחשפים **אך ורק נתונים** — **לעולם לא** כתיבה חופשית (החזון פרטי לחלוטין — ראה סעיף 3).

---

# נספח ב': רשימת החלטות פתוחות לעתיד
- כיוונון מדויק של בונוסי הרצף (מספרים).
- אילו כרטיסי נתונים בדיוק בדשבורד הנתונים (משולב בתוך מסך "הרגלים").
- שאלות מנחות במסך החזון.
- מנגנון בחירת נראות נתונים פר‑משתמש (אם "משתתפים" יחזור).
- החלפת Lucide במאגר אייקונים מותאם.
- מימוש חסימה אמיתית במעבר ל‑native (אם "חוסם" יחזור).
