// Placeholder; the real screen lives in features/admin/AdminScreen.tsx
// to keep all admin-related code under one folder. This file just re-exports
// so the route definition stays consistent with the other screens/ pattern.
export { AdminScreen as Admin } from '../features/admin/AdminScreen';
